name: CI/CD - Weather Dashboard

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: "abhanedhjies"
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: ghcr.io/${{ secrets.GHCR_USERNAME }}/weather-backend:latest

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: ghcr.io/${{ secrets.GHCR_USERNAME }}/weather-frontend:latest

      - name: (Optional) Deploy using Ansible
        if: ${{ github.ref == 'refs/heads/main' }}
        run: |
          echo "Deploy step placeholder"
          # For local/VM deployment, you could SSH into server and run:
          # ansible-playbook deploy.yml
